import { StyleSheet } from "react-native";

export default StyleSheet.create({
    container: {
        flex:1,
        alignItems: 'center',
        backgroundColor: 'red',
    },
    title: {
        color: 'black',
        fontSize: 25,
        fontWeight: 'bold',
    },
    message: {
        fontSize: 17,
        color: 'white',
    }
});